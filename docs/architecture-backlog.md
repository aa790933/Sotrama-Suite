# Architecture Backlog — Sotrama Suite

> Single source of truth for deepening work. Update at end of every slice, unprompted.
> Evidence is cited, not claimed. Check `git log`, file existence, and `grep` before marking done.

- [x] **ADR-0001 — Drop SQLite, MariaDB-only** — Done: `docs/adr/0001-drop-sqlite-mariadb-only.md:1` (Status: Accepted, 4.3K, 2026-08-30) exists; `src/utils/db.ts:1` is 12 lines / 528 bytes with no `dbErrorActionSymbols`/`DirectoryDoesNotExist`/`UnableToAcquireConnection` (grep `better-sqlite3|DirectoryDoesNotExist` → 0 hits in `src/`/`main/`/`backend/`; only `docs/adr` mentions it historically). Validated by `git log --oneline -3` → `a5fcd58`, `424153b`.

- [x] **Candidate 1 — Deepen Main-process Ipc router** — Done: `main/ipc/router.ts:1` is 599 lines with four typed sub-interfaces `DbOps`/`InstallerOps`/`FileOps`/`AppOps` (`router.ts:97-128`), every `ipc.handle` (31/31) wrapped with `withSender` (`router.ts:524-580`), `asDatabase(databaseManager): Database` (`router.ts:22-48`) injects real `fyo/database/Database.ts:25`, `sanitizeDatabaseName` rejects (`policies.ts:9-14`); **ConnectionStore seam** `main/ipc/connectionStore.ts:1` (75 lines) with `ElectronStoreConnectionStore` (prod) + `InMemoryConnectionStore` (test) decouples `router.ts:16` → `main/helpers.ts:5` → `utils/config.ts:1` → `electron-store` chain (now lazy, no top-level `require('electron')`); **real seam proof** `main/ipc/router.spec.ts:147` now has 53 tests including `IpcRouter + InMemoryConnectionStore — real seam (DbOps via InMemory, no electron mock)` (`router.spec.ts:147-189`) that constructs `IpcRouter` with `InMemoryConnectionStore` + `makeFakeDatabase()` and asserts `dbCall` through the `Database` seam without `Module._cache` mock. Building block for Host provisioning (needs `PersistedConnection`) and A+3. Commits `424153b` + `a5fcd58` + `router.spec` fix + `f41b38b`.

- [x] **Candidate A+3 — Database seam collapse & MainDatabase lifecycle** — Done: `backend/database/bespoke.ts` deleted (9 dead statics; finance/return/POS already recomposed in domain modules), `backend/database/manager.ts` deleted (lifecycle `createNewDatabase`/`connectToDatabase`/`connectInternal`/`setDbConfig`/`setRawCustomFields`/`#migrateLifecycle`/`#executeMigration`/`#createBackup` folded into `backend/database/core.ts:DatabaseCore`, `pool` private + `isConnected`), `call`/`callBespoke`/`DB_BESPOKE`/`databaseMethodSet`/`asDatabase` deleted, `main/ipc/router.ts:dbCall` is a typed `switch` over `DatabaseMethod` on `DatabaseCore` with `setDbConfig` wired from the resolved `Connection`, `Patch.execute` takes `DatabaseCore` (9 patches + `runPatch` repointed), `backend/helpers.ts` drops `databaseMethodSet`/`checkFileAccess`/`unlinkIfExists`. Known debt carried to 5+B: `getStockQuantity` still lives on the generic persistence seam (`core.ts`, `Database.ts`, `utils/db/types.ts`) instead of a `StockLedger` module.

- [x] **Debt: getStockQuantity → StockLedger seam (Candidate 5+B / Candidate 2)** — Done: new `models/inventory/StockLedger.ts` (`getQuantity`/`validateAvailability`/`getCOGS`/`getReturnBalance` over `StockLedgerEntry` via `getAll`/`getAllRaw` + `StockQueue` FIFO); `getStockQuantity` deleted from `DatabaseCore`, `Database`, `DatabaseBase`, both adapters, `DemuxDatabaseAdapter`/`DatabaseHandler`, and the IPC `dbCall` switch (`StockQuery` types deleted); callers repointed (`StockManager` validates via `StockLedger`, `StockTransfer` COGS/returns via `StockLedger`, POS/UI/Invoice/tests via `getQuantity`); `Invoice.getReturnDoc` dead second branch (unreachable `if ({})` + `break`) and its wasted `getReturnBalanceItemsQty` query deleted, leaving one live caller per return-balance shape. `npx tsc --noEmit` (0 errors).

- [ ] **Candidate 5+B — StockService / StockLedger consolidation** — Not started: no `StockService` file (`grep -rn StockService --include="*.ts" models/ → 0`), `models/inventory/StockManager.ts:1` still 8.8K with `deleteAll(StockLedgerEntry)` and `#getStockLedgerEntry`, `reports/inventory/helpers.ts:19` still owns `getRawStockLedgerEntries`/`StockQueue` FIFO, `src/utils/pos.ts` still imports from reports. No commit touches `models/inventory/*` for this.

- [x] **Candidate 2 — Host provisioning / MariaDB installer state machine** — Done: `main/mariadbInstall.ts` (908 lines) deleted, split into `main/provision/hostProvision.ts` (`provision(config,onProgress?)` owning port probe, install, host-mode bind/firewall, GRANT scope, app-user verify) + `main/provision/platformInstallers.ts` (`PlatformInstaller` seam with Windows/macOS/Linux adapters; the 3 `switch(platform)` blocks are one `forPlatform` dispatch); `main/ipc/router.ts` `installer` struct + lazy requires deleted, `InstallerOps` is `provisionMariaDB` + validation ops (`ping`/`checkDbExists`/`createDatabase`/`getLanIp`), `DOWNLOAD_MARIADB_INSTALLER`/`IS_PORT_AVAILABLE` channels + `resolveMsiPath`/`isPortAvailable`/`downloadMariaDBInstaller` deleted, `HostProvisionConfig`/`InstallProgress` in `utils/mariadb-types.ts` (`InstallOptions` deleted, `InstallResult.port?` added); `HostSetup.vue` express flow is one `provisionMariaDB` intent with staged progress (port loop, `win32` download branch, manual re-check, app-user ping moved to main). `npx tsc --noEmit` (0 errors).

- [x] **Candidate 4 — Connection identity collapse** — Done: `equalsConnection` (host+port+database+user) + `toSafeDisplay` canonical in `utils/mariadb-types.ts`, repointed in `toConnectionMetadata`/`findConnectionByConfig`/both store adapters/preload/`misc.updateConfigFiles`/`App.vue`; `ConnectionStore.resolve` unifies id-or-JSON→config (router drops both private resolvers); deleted `setAndGetCleanedConfigFiles`/`getConfigFilesWithModified`/`migrateLegacyConnections`/`migrateLegacy`/`rawJsonForLegacy` + files-list `getDbList` fallback; `ConnectionConfig` is now an alias of `MariaDBConfig`. Renderer boot flow (`App.vue`/`DatabaseSelector`/`misc.updateConfigFiles` custody) intentionally untouched. `npx tsc --noEmit` (0 errors).

- [x] **Candidate 4b — Doc↔Raw conversion / FieldTypeRegistry** — Done: `fyo/core/converter.ts` switches replaced by `FieldTypeDescriptor` registry (`Partial<Record<FieldType, …>>`, string fallbacks preserved); legacy `Converter(db, fyo)` object-construction branch + `db?`/`fyo?` fields deleted (all 8 call sites already provider-based). `DemuxDatabaseAdapter` shim kept per `CONTEXT.md` until `leaveBalance` migrated; `schemas/index.ts` flag chain left as-is (total function, no depth gain from tabling).

- [x] **Candidate 6 — SQLite dead-code deletion (quick pass)** — Done (incidental): `src/utils/db.ts:1` shrank 98→12 lines (87 deleted) removing `dbErrors`/`handleDirectoryDoesNotExist`/`handleUnableToAcquireConnection`/`showDbErrorDialog`; `grep -r better-sqlite3|DirectoryDoesNotExist|UnableToAcquireConnection --exclude-dir=node_modules → 0` in code (only `docs/adr` historically). FieldType/domain cohesion part still Not started (see above; `models/helpers.ts:1` still 1779 lines, `schemas/index.ts:12` still has 4 switches).

- [x] **Candidate C — PricingEngine extraction** — Done: `models/pricing/PricingEngine.ts` (pure `lineDiscount`/`lineDiscountForTax`/`totalItemDiscount`/`singleItemDiscount`/`invoiceDiscountAmount`/`totalDiscount`/`lineTax`/`summarizeTaxes`/`sumWithTaxes`); `Invoice` totals/tax/discount/grandTotal + `getLPAddedBaseGrandTotal` base delegate to it (signatures kept, arithmetic transcribed exactly); `StockTransfer` delegates with the `mul(quantity)` set-amount fix and real `isReturn` flags; `updatePricingRule` applies inline (no `setTimeout`); rule fetching batched (`batchPricingRuleDocNames` + union `PricingRule`/`CouponCode` fetches in both orchestrations, `itemQuantity` hoisted). `pos.getItemDiscounts` keeps its display guards (`percent > 1`); loyalty-point I/O stays on `Invoice`. `npx tsc --noEmit` (0 errors).

- [x] **Candidate D — AccountingLedgerSummary** — Done: `reports/finance/ledgerSummary.ts` (`getLedgerEntries` always `reverted:false`, inclusive JS date bounds, `monthKey`, `parseAmount`, `getDocTotals`); all 5 reports delegate with unchanged signatures (fixes: `creditDebit` gains the `reverted` filter, `between` filters that threw `Unsupported SQL operator` on MariaDB now apply in JS, `monthKey` handles `Date` objects). `posTransacted` already domain-owned via `getAll`, left untouched. `npx tsc --noEmit` (0 errors).

---

**Top next:** Tracks A+B+C landed → 5+B remainder still open (`StockService` file, `reports/inventory/helpers.ts` ownership, `src/utils/pos.ts` display helper). All other candidates done.

*Last verified: 2026-09-02 via `git log --oneline -5` (`181f1d3`, `f41b38b`, `a5fcd58`, `424153b`, `6eced74`), `ls backend/database/bespoke.ts` (deleted, 0 hits), `ls backend/database/manager.ts` (deleted, 0), `ls backend/database/core.ts` (1575 lines, admin+domain, `grep -n "TODO(Candidate 2)" backend/database/core.ts:655` → 1), `grep -rn "callBespoke|DB_BESPOKE" --include="*.ts" → 0 code hits (only backlog comment), `grep -rn "checkFileAccess|unlinkIfExists" --include="*.ts" → 0 (helpers.ts 69 lines, no fs.access)`, `npx tsc --noEmit` (0, no errors), `seriesConcurrency` 13/13 (FOR UPDATE, 10 unique, current=1010), `router.spec` 67/67 (DatabaseCore direct), `testCore` 328/328, `grep -r sqlite` (0 code hits).*
